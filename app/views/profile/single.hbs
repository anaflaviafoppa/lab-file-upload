<h1>Single Perfil</h1>

<h1>{{ profile.name }}</h1>
<img src="{{profile.picture}}" alt="{{profile.name}}">

{{#if isOwnProfile}}
  <a href="/profile/edit">Edit Profile</a>
{{/if}}

{{#each posts}}
  {{>posts}}
{{/each}}
